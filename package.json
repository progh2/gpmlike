{
  "name": "gpmlike",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Nuri Term — original GPM-inspired academy + mecha-lite web game",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@pixiv/three-vrm": "^3.5.5",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
